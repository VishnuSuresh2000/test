import 'package:flutter/material.dart';

class BeruMaintance extends StatelessWidget {
  const BeruMaintance({
    Key key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text("Under Constrcution",style: TextStyle(
          color: Colors.red,
          fontSize: 30
        ),),
      ),
    );
  }
}